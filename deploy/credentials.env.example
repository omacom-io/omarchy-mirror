# R2 S3 credentials with object read/write access to the preview bucket.
# Keep this file root-readable only; Docker passes the values to the publisher.
AWS_ACCESS_KEY_ID=REPLACE_ME
AWS_SECRET_ACCESS_KEY=REPLACE_ME
AWS_DEFAULT_REGION=auto
