#!/bin/bash

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
"$SCRIPT_DIR/omarchy-mirror-sync" --upstream "mirrors.kernel.org/archlinux/" --stage "/mnt/arch-mirror-rc/" --r2 "omarchy:omarchy-mirror-rc"
