#!/usr/bin/env python3
"""Run from a checkout or install the console entry point with pip."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from mirror.cli import main

if __name__ == "__main__":
    main()
